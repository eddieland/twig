[workspace]

[package]
name = "twig-deploy"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "twig-deploy"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
twig-core = { path = "../../../twig-core" }
